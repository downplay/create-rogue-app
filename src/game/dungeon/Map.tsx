import React, {memo}

type Props = {
    grid: Grid;
  };
 
const MapRow = ({row}) => {

}

export const Map = () => {
    ({ grid }) => {
        grid.map((row,index)=></row.map(cell => ))
      };
}