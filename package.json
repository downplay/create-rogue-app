{
  "name": "create-rogue-app-workspaces",
  "version": "0.3.0",
  "private": true,
  "workspaces": ["packages/herotext", "games/osa", "games/hero"],
  "scripts": {
    "start": "cd games/hero && yarn start",
    "build": "yarn workspaces run build",
    "osa": "cd games/osa && yarn start"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "npm-run-all": "^4.1.5"
  }
}
